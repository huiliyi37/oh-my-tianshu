# Minimal tool-pwsh composition: real app boot path, real pwsh executor, real
# foreground + background tool calls; driven by the package's loader.spec.ts.
- id: system-prompt
  name: '@huiliyi37/dsh-system-prompt'

- id: tools
  name: '@huiliyi37/dsh-tools'

- id: subprocess
  name: '@huiliyi37/dsh-subprocess-local'

- id: bash
  name: '@huiliyi37/dsh-pwsh-local'
  config:
    graceMs: 200

- id: bash-env
  name: '@huiliyi37/dsh-bash-env'

- id: tasks
  name: '@huiliyi37/dsh-tasks-local'

- id: tool-tasks
  name: '@huiliyi37/dsh-tool-tasks'

- id: tool-pwsh
  name: '@huiliyi37/dsh-tool-pwsh'
